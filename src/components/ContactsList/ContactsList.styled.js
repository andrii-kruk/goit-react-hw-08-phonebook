import styled from 'styled-components';

export const StyledList = styled.ul`
  width: 800px;
  margin-left: auto;
  margin-right: auto;

  display: flex;
  flex-wrap: wrap;
  justify-content: center;

  row-gap: 4px;
`;
