import styled from 'styled-components';

export const StyledSection = styled.section`
  padding-top: 48px;
  padding-bottom: 48px;
`;

export const Styledtitle = styled.h1`
  font-weight: 500;
  font-size: 40px;
  line-height: 1.2;
  letter-spacing: 0.04em;
  text-align: center;
  margin-bottom: 24px;
`;

export const StyledDescription = styled.h2`  font-weight: 500;
font-size: 32px;
line-height: 1.2;
letter-spacing: 0.04em;

margin-bottom: 50px;

text-align: center;
}`;

export const StyledList = styled.ul`
  width: 450px;
  margin-left: auto;
  margin-right: auto;
  margin-bottom: 36px;

  display: flex;
  flex-direction: column;
  gap: 16px;

  font-size: 18px;
  line-height: 1.6;
  letter-spacing: 0.02em;
`;

export const StyledItem = styled.li``;
