import { Input } from 'antd';
import styled from 'styled-components';

export const StyledFilter = styled(Input)`
  display: block;
  width: 320px;

  margin-left: auto;
  margin-right: auto;

  margin-bottom: 24px;
`;
